const GET_KENNEL = 'kennel/GET_KENNEL';

const getKennel = (kennel) => ({
    type: GET_KENNEL,
    kennel
})

const initialState = {};

export const getUserKennel = () => async (dispatch) => {
    
}